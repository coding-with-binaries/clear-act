{
  "private": true,
  "workspaces": [
    "lib",
    "doc"
  ],
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "scripts": {
    "start": "concurrently \"yarn --cwd doc start\" \"yarn --cwd lib start\""
  }
}
